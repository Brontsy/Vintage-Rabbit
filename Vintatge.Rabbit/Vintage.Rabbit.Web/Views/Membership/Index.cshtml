@model Vintage.Rabbit.Web.Models.Membership.LoginRegisterViewModel
@{
    ViewBag.Title = "Login / Register";
    Layout = "../Shared/_Layout.cshtml";
}


<div class="login-register-page">
    <div class="row">
        <div class="col-5">            
            @{Html.RenderPartial("Register", Model.Register);}
        </div>
        <div class="col-2">
            <div class="line">
                <div class="or">Or</div>
            </div>
        </div>
        <div class="col-5">
            @{Html.RenderPartial("Login", Model.Login);}
        </div>
    </div>
</div>
